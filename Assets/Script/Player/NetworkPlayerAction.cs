using System;
using System.Collections;
using System.Collections.Generic;
using Script;
using UnityEngine;

/*
public class NetworkPlayerAction : NetworkBehaviour
{
    [SerializeField] private Vector2 speedMagnitude = new Vector2(5.0f, 3.0f);

    private ChampionState _state;
    private bool isJumping = false;
    PlayerMoveController _playerMoveController;
    private Animator animator;


    void Start()
    {
       
        _state = ChampionState.IDLE;
    }

    

    void Update()
    {
        if (!hasAuthority)
            return;
        
       // _playerMoveController.moveUpdate();
    }

    private void LateUpdate()
    {
        if (!hasAuthority)
            return;
      //  _playerMoveController.moveLatedUpdated();
    }
}*/